# SD-Renderer-PyPi-Package

# step1: Virtual env create
conda create -n renderer python=3.8 -y

conda activate renderer

# Step 2:Folder Structer Creation
Project name: SDRenderer
then run the template folder fike

# 
write code in steup.py
setup.cfg
project.toml
run -r requirements_dev.txt
pip install -r requirements_dev.txt

